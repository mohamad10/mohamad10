/* تنظیمات اتصال به بک‌اند لاراول.
 * apiUrl خالی باشد → سایت فقط از assets/data.js می‌خواند (حالت استاتیک).
 * اگر API در دسترس نباشد، سایت خودکار از data.js استفاده می‌کند. */
window.SITE_CONFIG = {
  apiUrl: 'http://127.0.0.1:8000/api'
};
